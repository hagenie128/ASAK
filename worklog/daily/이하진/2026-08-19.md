# 2026-08-19 일일 워크로그 — 이하진

> ASAK 9주 프로젝트 (7/2~9/2 발표)
> daily 형식 기준: [2026-08-18](2026-08-18.md)

---

## 오늘 요약

| 담당자 | 저장소 | 작업 | WBS / Issue | 상태 | 블로커 |
|---|---|---|---|---|---|
| 이하진 | ASAK-back / ASAK-Admin | 관리자 매출 API·DTO·매퍼 구현, 기기 출력 이벤트 API 추가, null safety 수정, 스키마 문서 동기화, TODO 재정리, Admin READY 버튼 스타일·발표 전 전체화면 비활성화 | Issue 없음 | ✅ 커밋·merge·원격 반영 / ✅ summary 런타임 조회 확인 / ⚠️ monthly 집계 스텁 | monthly 실제 집계 미구현 |
| 이하진 | ASAK / ASAK-back | DevCopilot API·DB 설계 정렬, 문자열 업무 코드 규칙 문서화, PDF 대조 기반 메뉴·옵션 운영 결정 기록 | Issue 없음 | ✅ 문서·허브 반영 / ⏳ 코드·DB 반영 대기 | 옵션 `active` 필터 코드·DB 갱신 미실행 |

## 오늘 작업

### 관리자 매출 API 구현 — `feat` · ASAK-back

- **작업 목적:** 관리자 시간별·일별·월별 매출 조회 엔드포인트와 DTO, MyBatis 매퍼를 구현한다.
- **직접 구현:** DTO 구조 정리(`f0d9c09`), 매퍼 전환(`cf819b0`), 조건 추가(`ae9ab5e`), 파라미터 보완(`0eae52a`), 월별 매출 엔드포인트(`6755aa8`), Admin DTO 패키지·Sales Mapper 뷰 매핑 수정(`471ff80`), merge(`79c4581`). API 구현 규칙 문서(`a631176`).
- **AI 도움:** Cursor. DTO 설계, 매퍼 XML 초안, 문서 정리 보조.
- **이슈:** DB 뷰(view) 기반 매출 집계. 월별 endpoint는 현재 빈 배열을 반환하는 스텁이다.
- **검증:** `compileJava` 성공. 검사 전용 포트 `18080`에서 `GET /api/admin/sales/summary?startDate=2026-08-01&endDate=2026-08-19`가 200·`ADMIN_SALES_SUMMARY_SUCCESS`·531건을 반환했다. 날짜 역전 요청은 200·`END_DATE_LESS_THAN_START_DATE`를 반환했다. `GET /api/admin/sales/monthly?year=2026`는 200·빈 배열로 확인되어 실제 월별 집계 검증은 미완료다.
- **상세 기록:** [매출 API 런타임 검증](../../entries/이하진/2026-08-19-admin-sales-runtime-verification.md)

### 기기 출력 이벤트 API — `feat` · ASAK-back

- **작업 목적:** 영수증 재출력, RTOS 폴링 등 기기 출력 이벤트를 수신하는 엔드포인트를 추가한다.
- **직접 구현:** 커밋 `26451f0`.
- **검증:** 빌드 성공. 런타임 미확인.

### null safety·스키마 문서 동기화 — `fix` / `docs` · ASAK-back

- **작업 목적:** null type safety 경고를 수정하고, DB 스키마 문서를 실제 DDL과 동기화한다.
- **직접 구현:** null safety(`f8978dc`, merge `fa7102b`), 스키마 검증 도구·구현 규칙(`c68b1f1`, `3fc21b5`, merge `bb58787`).
- **검증:** 빌드 경고 제거 확인.

### DevCopilot API·DB 설계 및 운영 결정 정리 — `docs` · ASAK / ASAK-back

- **작업 목적:** 로컬 정본, 실제 운영 DB, DevCopilot Hub의 API 번호·오류 응답 규칙·DB 설계 기록을 맞추고, PDF 대조 결과를 무분별한 삭제가 아닌 운영 판단 기준으로 남긴다.
- **직접 확인·반영:**
  - Hub 기존 API 카드 `430~435`에 `API-025~030` 번호를 정렬하고, API 카드 30개에 문자열 업무 `code`와 공통 오류 envelope 규칙을 기록했다.
  - 실제 운영 DB와 Hub 운영 Base Table 26개를 대조했다. 누락·타입·NULL·PK·FK 불일치는 없었고, Hub `ing`의 레거시 영양 컬럼 6개는 삭제 보류했다.
  - `04-api-db-implementation.md`에 `status`와 문자열 업무 `code`의 역할을 분리해 기록했다.
  - PDF 미수록만으로 메뉴·옵션을 삭제하지 않는 기준, 깨진 「빼기」 옵션 4개·드레싱·토핑의 `opt_policy_item.active` 처리, 메뉴 15개의 `deleted_at` soft delete 후보 처리 기준을 문서화했다.
- **AI 도움:** DevCopilot API·DB 대조, 실제 DDL·Mapper 근거 확인, 문서·작업 기록 정리.
- **검증:** Hub API 카드·DB 스키마 재조회 및 문서 공백 검사. 실제 메뉴/옵션 DB 갱신, 키오스크·주문 API E2E는 실행하지 않았다.
- **관련 문서:** [업무 코드 규칙](../../../docs/implementation_guide/04-api-db-implementation.md), [PDF 대조 운영 결정](../../../../ASAK-back/docs/2026-08-19_pdf_intersection_cleanup_plan.md), [Hub DB 동기화 기록](../../../docs/ai-reports/2026-08-19/asak-doc-sync-hub-db-schema.md).
- **Git 근거:** ASAK 문서·Hub 동기화(`44ac24c`, `d8ab908`), 시드·옵션 정책 근거 보완(`35d66d2`, `89051bc`), 스키마 실측 근거 보완(`9487ec7`, `5ea8d10`). 커밋은 문서 반영 근거이며, API·DB 런타임 검증 근거는 아니다.

### TODO 재정리 — `chore` · ASAK-back / ASAK-Admin

- **작업 목적:** 인라인 TODO-NNN 번호를 구현 순서대로 001~043으로 재정리한다.
- **직접 구현:** back(`6f4201f`), Admin(`c08190f`).
- **검증:** 코드 정리만.

### Admin READY 버튼 스타일·발표 전 전체화면 비활성화 — `fix` · ASAK-Admin

- **작업 목적:** READY 상태 주문 버튼에 스타일을 추가하고, 발표 전 시작하기 전체화면 전환을 비활성화한다.
- **직접 구현:** READY 버튼(`6730e0b`, merge `06e1c63`), 전체화면(`7b8fceb`, merge `a6d2840`).
- **검증:** 코드 수준 확인. 브라우저 UI 미확인. Figma 미확인.

## AI 활용 메모

- 매출 API DTO·매퍼·엔드포인트 설계, null safety, 스키마 문서 동기화, TODO 재정리
- `summary`는 컴파일·로컬 기동·실 DB 조회까지 확인했다. `monthly`는 빈 배열 스텁이므로 실제 집계 구현·검증과 구분한다.

### RTOS 영수증 출력 별도 저장소 — `feat` (신규 repo)

- **작업 목적:** 영수증 재출력·RTOS 폴링 관련 임시 코드를 별도 Git 저장소에 넣고 나연님을 초대한다.
- **직접 구현:** 새 저장소 생성·나연님 collaborator 추가. 선생님 예제 파일 기반 코드.
- **검증:** 저장소 생성·초대 확인. 코드 동작 미검증.

### 옵션 중복 DB 정리·운영 결정 — `fix` · ASAK-back (DB)

- **작업 목적:** 옥수수 등 옵션 아이템이 2개씩 들어가 있는 중복 데이터를 정리하고, 깨진 「빼기」 옵션 4개·단종 후보 15개·드레싱·토핑 비노출 기준을 문서화한다.
- **직접 구현:** `2026-08-19_pdf_intersection_cleanup_plan.md`에 처리 기준 문서화. `opt_policy_item.active` 필터를 코드에 반영하는 방안 정리. DB 직접 수정은 팀 합의 후.
- **검증:** 문서 작성만. 실 DB 갱신·코드 반영 미실행.

### DB 뷰 22개 레퍼런스 정리 — `docs` · ASAK-back

- **작업 목적:** 키오스크·관리자가 사용할 DB 뷰 22개의 목적·화면별 조회 흐름을 HTML 문서로 정리한다.
- **직접 구현:** `asak_view_reference.html` 작성. `vw_menu_list`, `vw_menu_availability`, `vw_menu_opt_policy_json`, `vw_payment_result` 등 핵심 뷰와 화면 매핑 포함.
- **검증:** 문서 작성만. 뷰 존재·데이터 확인 미실행.

## 블로커 / 공유 사항

- 매출 API — `summary` DB 뷰 조회는 확인했으나, `monthly`는 빈 배열 스텁이다. Admin 화면 연동·브라우저 E2E는 미실행.
- `orderSessionStore.js` 로컬 변경 미커밋(Kiosk).
- 옵션 `active=0`은 현재 `UserMenuMapper`·`UserOrderMapper`가 `opi.active`를 필터링하지 않아 DB 값만 바꿔서는 화면·주문에 반영되지 않음. 코드 수정 승인과 회귀 확인 후 처리 필요.
- `.env` 보안 키(Cloudinary API Secret 등) 카톡 공유됨 — 프로젝트 종료 후 키 로테이션 필요.

## 내일 계획

- 월별 매출 집계 구현 후 API·Admin 화면 연동 검증.
- 기기 출력 이벤트 API 테스트.
- `UserMenuMapper`·`UserOrderMapper`의 옵션 조회·검증에 `opi.active=1`을 반영한 뒤, 깨진 「빼기」 옵션 4개와 드레싱·토핑의 비노출 처리를 검증.
- 발표 준비.
