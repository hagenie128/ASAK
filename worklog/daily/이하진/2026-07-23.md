# 2026-07-23 일일 워크로그 — 이하진

> ASAK 9주 프로젝트 (7/2~9/2 발표)

---

## 오늘 요약

| 담당자 | 저장소 | 작업 | WBS / Issue | 상태 | 블로커 |
|---|---|---|---|---|---|
| 이하진 | ASAK-Admin | 매출·메뉴·대시보드 mock, Page=조합, Figma 달력/컴포넌트·결제수단 정합, 셸 스케일·P1 Async/Confirm, 품절·메뉴 카드 UI 후속 수정, 브랜치 push | WBS2-039~043 / SCR-016·018~022 | ✅ 대량 완료 (품절·메뉴 카드 UI 후속 수정 포함) | Backend business API 미구현 |

## 오늘 작업

### 매출 3화면 mock + 달력 필터 — SCR-019~021 · WBS2-041~043 · `feature`

- **작업 목적:** 어제까지 정적/문서만 있던 매출 요약·월별·일별을 mock repository에 연결하고, Figma 기간/일자 네비를 동작시킨다.
- **직접 구현:**
  - `useSalesQuery` · `salesDisplay` 유틸 · `SalesSummaryPage` / `MonthlySalesPage` / `DailySalesPage` mock 바인딩
  - `AdminDatePicker` — **single**(한달 달력) / **range**(두달 + 프리셋)
  - 기간 탭 · 일별 연간 달력 선택 · 일별 라벨 `YYYY.MM.DD (요일)`
  - 캘린더 CSS: 한달 날짜가 전부 라임 원으로 보이던 버그 → `sales-period.css`의 `.sales-daily__date span`이 **팝오버 DatePicker 안까지 덮친(누수)** 것이 원인. 자손 `span` 규칙 정리 + picker 쪽 리셋
- **AI 도움:** AI 어시스턴트에 Page=조합·달력 시안 대조를 맡기고, Admin 전체 프레임을 DatePicker로 착각한 경우 노드 URL을 다시 주고 재조회시켰다. “한달 전부 라임”은 컴포넌트 로직이 아니라 **CSS 특이도 누수**로 가설을 좁혀 달라고 지시했고, `sales-period` → picker 경로를 추적한 수정만 채택했다.
- **이슈:** 일별 mock 상세는 7월만 — 다른 달은 빈 상태 안내. range 팝오버(약 625px)는 좁은 창에서 잘릴 수 있음.
- **검증:** `npm run build` 반복 성공. 일별/요약 달력 수동 확인.
- **포트폴리오 요약:** 매출 화면을 mock 기간·일자 필터와 Figma DatePicker(한달/두달) 스타일로 연결하고, CSS 누수 버그를 원인 추적해 고쳤다.
- **상세 기록 →** [2026-07-23 Admin mock·Figma 정합](../../entries/이하진/2026-07-23-admin-mock-figma-parity.md)

### 메뉴 관리 조합 + 우측 Detail/Edit — SCR-016 · WBS2-039 · `feature`/`refactor`

- **작업 목적:** Page가 UI를 전부 그리던 구조를 `OrderListPage`처럼 **조합(assembly)만** 하도록 바꾸고, Figma상 별도 빈 페이지가 아닌 **우측 패널**로 신규/수정을 연다.
- **직접 구현:**
  - `useMenusQuery` · `MenuListPanel` · `MenuDetailPanel` · `MenuEditPanel`
  - `MenuManagePage` = thin assembly · `MenuEditPage`는 모드 wrapper
  - `IngredientSelectModal` (Figma `150:5525`) 연결
- **AI 도움:** “Page=조합” 규칙과 Figma Detail Add/Edit 노드를 기준으로 패널 분리·CSS를 요청. 저장은 stub toast로 유지하도록 범위를 제한했다. 전체 CRUD API를 한꺼번에 만들려는 초안은 거절.
- **이슈:** 메뉴 저장/삭제 실API·마스터 재료 API 미연동. 메뉴 카드 이름 2줄 높이·가격 nowrap은 **당일 후속 수정으로 완료**.
- **검증:** build 성공. `/menus`에서 보기/수정/신규 전환 확인.
- **포트폴리오 요약:** Admin 메뉴 워크스페이스를 목록+우측 상세/편집 조합 패턴으로 재구성했다.

### 대시보드·주문·품절 레이아웃/데이터 — SCR-022 · SCR-010 · SCR-011 · `feature`/`fix`

- **작업 목적:** 대시보드 섹션 조합화, 최근 주문을 주문 관리 소스와 맞추고, 품절 가로 배치·스크롤·페이지네이션을 정리한다.
- **직접 구현:**
  - `DashboardPanels` · `SalesShareCard`
  - 최근 주문 → `getAdminOrders` 최신 3건 매핑 (주문 관리와 동일 소스)
  - 품절 `@media` 브레이크 1500→1100, 화면별 `ADMIN_PAGINATION` 상수
  - 주문 목록 **상태 변경 UI 제거**(표시·필터만)
  - Admin 셸 **1920×1080 캔버스 + viewport `scale`** (뷰포트만 키우면 UI가 커 보이던 문제 대응)
- **AI 도움:** “뷰포트만 채우면 커 보인다”는 피드백에 캔버스 스케일 방식으로 전환하도록 지시. 카드가 불필요하게 늘어나지 않게 `align-items: flex-start`를 명시했다.
- **이슈:** 품절 카드 truncation(2줄 clamp)·카테고리 배지 다양화는 **당일 후속 수정으로 완료** (mock 재생성 + menus sync). 저장 stub가 `menus.isSoldOut`을 갱신하지 않는 점은 잔여.
- **검증:** build · 대시보드/품절/주문 화면 수동 확인.
- **포트폴리오 요약:** 운영 홈·품절·주문 목록의 레이아웃/데이터 경계를 mock 기준으로 정리했다.

### Figma 0718 컴포넌트·Shared·KEEP QA — `docs`/`feature`

- **작업 목적:** Figma `yHhvn5RKjBd91U8BJUQz7F` 정본 노드를 하나씩 대조해 빠진 UI를 가져오고, Admin Code Core(`3006:40067`) 전 페이지 갭을 정리한다.
- **직접 구현:**
  | 노드 | 반영 |
  |---|---|
  | DatePicker 한달 `162:15939` / 두달 `3218:16401` | 색·구조 (`#6c9700` 원/연녹 바) |
  | DetailPanel `150:5418` | default / empty / 취소 |
  | IngredientSelectModal `150:5525` | 메뉴 Edit 연결 |
  | Admin Components `150:2860` | StatusBadge · SearchInput · Checkbox + CSS 분리 |
  | Shared `145:2` | Toast · Confirm · AsyncState 뼈대 |
  | Foundations `148:12745` | `--admin-bg-inverse` |
  | State Matrix `191:3` | 문서용만 — UI 미가져오기 |
  | KEEP QA `3006:40067` | → `AdminAsyncState` (매출·대시보드 선반영 후 주문/품절/메뉴·결제수단까지 확장) |
- **AI 도움:** 노드 URL을 잘못 보면 피드백하고 재조회. CSS 누수(부모 `span` 선택자) 원인 추적에 AI를 리뷰 보조로 씀. 시안에 없는 empty/error를 발명하지 말라고 제한.
- **검증:** 다수 `npm run build` 통과.

### P1 — AdminAsyncState · ConfirmDialog · 주문 달력 연도 — `feature`

- **작업 목적:** KEEP QA에서 남아 있던 loading/empty/confirm UX를 공통 컴포넌트로 통일한다.
- **직접 구현:**
  - `AdminAsyncState`를 **주문·품절·메뉴**(및 결제수단) 페이지에 연결
  - `AdminConfirmDialog`를 저장/위험 액션 confirm에 연결
  - 주문 화면 달력 **연도 범위** 정리
- **AI 도움:** “화면마다 스피너 JSX를 복붙하지 말고 Shared AsyncState/Confirm만 쓰라”고 지시. 불필요한 새 추상화는 거절.
- **이슈:** 일부 empty 카피·레이아웃은 Figma MISSING이면 발명하지 않음.
- **검증:** 해당 페이지 loading/confirm 수동 확인 + build.

### 결제수단 SCR-018 mock·Figma 정합 — WBS2-040 후속 · `fix`/`feature`

- **작업 목적:** mock이 **8종**이던 것을 Figma SCR-018 Default(`134:11493`) 기준 **4종**(카드·카카오·네이버·제로)으로 맞추고, 미리보기·저장 UX를 Shared와 맞춘다.
- **직접 구현:**
  - mock 이름·순서 정렬: `card` → `kakao` → `naver` → `zero`
  - 미리보기 토글 · `AdminAsyncState` + `AdminConfirmDialog` 연결
  - `paymentMethodGlyphs` 등 표시용 상수 정리
- **AI 도움:** “Figma 4종만 남기고 나머지 mock 항목을 임의로 디자인하지 말라”고 지시. 초안의 장식성 아이콘/배지 추가는 거절하고 SCR-018 행 구조만 채택.
- **이슈:** 저장은 여전히 mock stub. 실 API 대기.
- **검증:** `/payment-methods`에서 4행·토글·저장 confirm/toast 확인.
- **포트폴리오 요약:** 운영 결제수단 화면을 Figma SCR-018 계약(4종·순서)과 Shared Async/Confirm에 맞췄다.

### CSS 체계·Git 브랜치·문서 — `refactor`/`docs`

- **작업 목적:** Admin CSS를 Kiosk와 같은 레이어로 맞추고, 작업 결과를 원격 브랜치에 올린다.
- **직접 구현:**
  - CSS: `tokens` → `reset` → `global` → `commonStyle` (+ date-picker, status-badge, filters-inputs, ingredient-select, payment-methods)
  - `STRUCTURE_GUIDE` Page=조합 규칙 보강
  - 임시 투두·체크리스트 정리 (로컬 개인 메모)
  - Git: 브랜치 `feature/admin-mock-figma-parity`를 **ASAK_Admin**에 push (**main 아님**)
- **검증:** build 성공. `app-shell.css`는 호환 re-export 유지.

## AI 활용 메모

- Figma → 코드는 **노드 URL을 내가 고르고**, AI에는 그 노드만 대조하게 했다. 잘못된 프레임을 보면 즉시 폐기·재조회.
- “발명 금지”: State Matrix·MISSING 프레임은 UI로 가져오지 않음.
- CSS 버그는 UI 로직 수정 제안보다 **선택자 누수** 가설을 먼저 검증하게 시켰다.
- P1은 새 컴포넌트 남발이 아니라 Shared(`145:2`) 재사용으로 범위를 좁혔다.
- 결제수단은 mock 개수를 Figma에 맞추는 게 핵심이라, 8→4 축소·순서 고정만 채택하고 장식은 거절.

## 블로커 / 공유 사항

- Backend business API 없음 → 실 adapter BLOCKED.
- Figma MISSING 상태 프레임은 디자인이 없어 코드로 발명하지 않음.
- Admin mock은 화면 연결·Shared UX·품절/메뉴 카드 UI 후속 수정까지 완료. 저장 stub·실 API는 잔여.

## 후속 수정 완료 (당일)

- 품절: 메뉴명 2줄 clamp, mock 재생성·menus sync로 카테고리 배지 다양화
- 메뉴 목록 카드: 이름 2줄 높이, 가격 nowrap + flex-shrink 0
- `npm run build` 통과 · [ASAK_Admin #4](https://github.com/hagenie128/ASAK_Admin/issues/4#issuecomment-5050010242)

## 내일 계획 / 잔여 이슈

- Backend business API stub (실 adapter BLOCKED)
- Admin↔Kiosk 결제수단 개수 불일치 (Admin 4종 vs Kiosk 8종) — 계약 재확인
- 품절 저장 stub가 `menus.isSoldOut`을 갱신하지 않음
- P2: 결제 정책 수정, Login Unauthorized, 메뉴 이미지 폴백
- DevCopilot/WBS Evidence 원격 반영
- (선택) `feature/admin-mock-figma-parity` PR 초안
