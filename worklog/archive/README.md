# Worklog archive boundary

`completed/`, `former-members/`, and `plans/` preserve the intended destination for concluded records. Existing dated records remain in `daily/` and `entries/` while the calendar and Notion sync scripts depend on those exact paths.

Former-member empty placeholders have been moved here; no worklog history was deleted.
