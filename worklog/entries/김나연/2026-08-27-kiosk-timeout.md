# 2026-08-27 키오스크 자동 타임아웃 — 김나연

> **일일 기록:** [2026-08-27 daily](../../daily/김나연/2026-08-27.md)  
> **템플릿:** [03-work-log-template.md](../../../docs/guides/03-work-log-template.md) · **예시:** [04-sample-work-log-example.md](../../../docs/guides/04-sample-work-log-example.md)  
> **근거:** Git `49107bc`, `75570c6`. **커밋 ≠ 태블릿 E2E 검증.**

---

## 1. 기본 정보

- 작업 날짜: 2026-08-27
- 담당자: 김나연
- 저장소: `ASAK-Kiosk`
- 브랜치: `ny/api-connection` (백업 커밋, merge 전)
- 관련 이슈/PR/화면: 키오스크 유휴 세션 정책
- 작업 유형: `feature`

## 2. 작업 목적

- 이번 작업에서 해결하려고 한 문제: 무인 키오스크에서 손님이 중간에 떠나면 장바구니·결제 세션이 남아 다음 손님 주문에 섞일 수 있다.
- 기대 결과: 일정 시간 입력이 없으면 홈으로 돌아가고 `orderSessionStore`가 초기화되어야 한다.

## 3. 직접 구현 영역

- 앱 레벨(또는 레이아웃) **유휴 타이머**를 추가했다.
- `pointerdown` / `keydown` 등 사용자 이벤트마다 타이머를 리셋하는 리스너를 붙였다.
- 타임아웃 시 `orderSessionStore` reset + `navigate('/')`로 초기 화면 복귀.
- 백업 브랜치에 2건 커밋 (`49107bc`, `75570c6`).

## 4. 구현 로직 / 적용한 방식

- 어떤 로직을 사용했는지:
  - `setTimeout` + 마지막 활동 시각(`lastActivityAt`) 비교, 또는 `useIdleTimer` 유사 패턴
- 왜 그 방식을 선택했는지:
  - 전역 store와 라우터만 건드리면 Cart·Payment·Complete 전 화면에 동일 정책 적용 가능
- 데이터 흐름 또는 처리 순서:
  - 사용자 입력 → 타이머 리셋 → (무입력 N초) → store clear → 홈 라우팅
- 핵심 조건문 / 예외 처리:
  - 결제 위젯 오픈 중 타임아웃 여부는 **미정** (태블릿 실측 TODO)

## 5. AI 도움 영역

- 사용한 AI 도구: (당일 구현 시점 기록 없음) · backfill 시 Cursor
- 어떤 질문/요청을 했는지: Git 커밋 메시지·파일명 기준으로 entry 12섹션 재작성
- 그대로 사용한 부분: 커밋 해시·저장소 경계
- 수정해서 사용한 부분: 타이머 구현 세부는 커밋 diff 미대조로 일반 서술

## 6. 발생 이슈

- 이슈 1:
  - 증상: (당일 기록 없음) 결제 진행 중 홈 복귀 가능성
  - 원인: 결제 SDK 모달 구간을 idle에서 제외하지 않았을 수 있음
  - 해결: 미적용 — 종강 후 태블릿에서 확인 예정

## 7. 디버깅 기록

- 확인한 로그/에러 메시지: backfill 시점 재실행 없음
- 다시 같은 문제가 생기면 먼저 볼 파일/명령어:
  - `orderSessionStore.js`
  - App/Layout 라우트 가드
  - `npm run dev` + 태블릿 Chrome 원격 디버깅

## 8. 이번 작업에서 배운 점

- 새로 배운 기술/개념: 키오스크는 **세션 초기화**가 주문 정합성과 직결된다.
- 다음 작업에 적용해보고 싶은 점: 결제·토스 콜백 구간은 idle 예외 목록에 넣기

## 9. 개선사항 / TODO

- 아직 임시 처리한 부분: 타임아웃 초·예외 화면 목록 하드코딩 가능성
- 다음 스프린트에서 개선할 부분: Product Bible 수치와 정책 문서 동기화
- 성능/구조적으로 아쉬운 부분: 커스텀 훅 `useKioskIdle`로 분리 검토

## 10. 검증 내용

- 실행한 명령어: (당일) `npm run dev` 수준 추정 · backfill에서 **재실행 안 함**
- 테스트한 시나리오: 미기록
- 확인 결과: **미검증** — 커밋만 존재

## 11. 포트폴리오용 요약

React 키오스크에 유휴 타임아웃을 넣어 무인 환경에서 주문 세션이 다음 고객에게 넘어가지 않도록 했다.

## 12. 첨부하면 좋은 자료

- 타임아웃 후 홈 복귀 화면 녹화
- `orderSessionStore` 초기화 전후 스크린샷
- 관련 커밋 `49107bc`, `75570c6`
