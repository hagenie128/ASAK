# ASAK Notion 다른 페이지 검토 — 2026-09-01

## 범위

Notion의 요구사항, 화면 설계, DB 설계, API 명세, 테스트/오류 관리, 팀 일정, 최종 제출 체크리스트, 종합 기획서를 현재 코드·QA 근거와 대조했다.

## 코드·QA 기준

- Admin 주문 상태 전이: `RECEIVED → PREPARING → COMPLETED`
- 승인 결제 환불: `PATCH /api/admin/orders/{orderId}/refund`, 환불 사유 body 필요
- 환불 트랜잭션: 결제 `APPROVED → REFUNDED`; 제공 전 주문은 `CANCELED`, `COMPLETED` 주문은 유지
- 2026-09-01 실DB 확인: 가상 CARD 주문 환불, 품절 저장·복구, 결제수단 PATCH 재조회·롤백, 환불 반영 매출 합계 대조
- 빌드: Backend Gradle test, Admin build 통과. Kiosk build·결제 E2E·RTOS 실실행은 미완료다.

## 우선순위별 발견 사항

| 우선 | Notion 페이지 | 문제 | 안전한 처리 방향 |
| --- | --- | --- | --- |
| P0 | 02. 요구사항 정의 | API 표가 옛 `/api/categories`, `/api/menus`, `/api/orders`, `/api/payments`, `/api/admin/sold-out-items` 경로다. 06. API 명세의 현재 `/api/kiosk/*`, `/api/admin/soldOut`과 충돌한다. | 표를 현재 API 명세로 교체하고, DB 요구사항 행은 일괄 완료 처리하지 않는다. |
| P0 | 06. API 명세 | 8/28 블록은 환불 HTTP/DB를 미검증으로 적고, `COMPLETED` 주문 유지와 제공 전 `CANCELED`를 설명한다. 9/1에는 제공 전 가상 CARD 환불 실DB가 확인됐다. | 기존 계약을 보존하고 9/1 QA 결과·검증 범위만 추가한다. API 번호 미배정은 팀 결정으로 남긴다. |
| P0 | 09. 테스트/오류 관리 | 8/28에 TC-001~017이 미실행으로 남아 있어 9/1 관리자 QA와 충돌한다. | TC 전체 PASS 선언 대신 ORD/REFUND·품절·결제수단·매출 대조의 실행 결과와 남은 Kiosk/RTOS 회귀를 추가한다. |
| P0 | 11. 최종 제출 체크리스트 | 장치 이벤트 API를 완료로 적고 React/Spring은 예정으로 적어 현재 근거와 어긋난다. RTOS 콘솔은 아직 실제 실행하지 않았다. | 제출 상태를 구현됨/검토중/미검증으로 분리하고 발표 전 체크리스트를 9/2 MVP 기준으로 교체한다. |
| P1 | 01. 팀/역할/일정 | 8/21 RTOS 최소 시연, 8/24~28 통합 테스트가 예정처럼 남아 있다. | 9/1 현재 결과와 9/2 당일 실행 항목(기동, 시연, 리허설)을 상단에 추가한다. |
| P1 | ASAK 종합 기획서 | 저장소가 과거 `ASAK-front` 중심이고 Week 5/초기 계획이 현재 실행 문맥과 섞여 있다. | 초기 기획 이력은 보존하고, 상단에 실제 활성 저장소 및 종강 MVP 범위·한계를 추가한다. |
| P1 | 05. DB 설계 | 초기 시드/22 테이블 설명이 중심이다. 현재 운영 DB, `payment_refund`, 매출 View, Mapper 기반 구현과의 차이를 표현하지 않는다. | ERD/시드 이력은 보존하고 실제 운영 DB 및 View의 차이를 별도 '현재 운영 기준' 블록으로 분리한다. |
| P2 | 04. 화면 설계 | Figma/Screen ID 기준을 유지하며 실제 브라우저 재검증을 완료로 주장하지 않아 상태 경계는 비교적 안전하다. | Admin·Kiosk 화면 E2E가 미검증임을 9/1 기준으로 추가하되 Figma 링크는 재확인 전 변경하지 않는다. |
| P2 | ASAK 처음 시작하기 | 과거 로컬 폴더/실행 환경 및 토큰 안내가 주된 내용이다. 기능 QA 상태와 직접 충돌하지 않는다. | 종강 후 인수인계 목적이라면 최신 저장소명·기동 순서만 별도로 재검증 후 갱신한다. |

## 결정 필요

1. API 번호 체계: 환불·환불 사유·RTOS API에 공식 번호를 배정할지 결정 필요.
2. 최종 제출 범위: RTOS를 '기본 구현 소스 존재'로만 제시할지, 실제 `make`·polling·finish 확인 후 시연 항목으로 올릴지 결정 필요.
3. DB 설계 정본: 초기 22 테이블 설계와 현재 운영 DB/View·`payment_refund`를 한 ERD에서 통합할지, 설계/운영을 분리할지 결정 필요.

## 권장 동기화 순서

1. 09. 테스트/오류 관리와 11. 최종 제출 체크리스트를 먼저 최신화한다.
2. 06. API 명세에 9/1 QA 보강 블록을 추가한다.
3. 02. 요구사항 정의의 옛 API 표를 현재 계약으로 교체한다.
4. 01. 팀/역할/일정과 종합 기획서에 종강 MVP 기준을 추가한다.
5. DB/화면 설계는 팀 결정 및 Figma·실DB 재확인 후 갱신한다.

## 2026-09-01 실제 반영

- `02. 요구사항 정의`: 과거 API 표를 현재 `/api/kiosk/*`, `/api/admin/*` 경로와 검증 상태로 교체했다.
- `06. API 명세`: 제공 전 환불의 실DB QA와 `COMPLETED` 주문 유지 정책을 분리해 추가했다.
- `09. 테스트/오류 관리`: 관리자 핵심 QA 실행 근거와 Kiosk/RTOS 미검증 경계를 추가했다.
- `11. 최종 제출 체크리스트`: 종강 MVP 판정 표를 추가해 Admin/Backend, Kiosk, RTOS, 실PG·장치의 상태를 분리했다.
- `01. 팀/역할/일정`, `ASAK 종합 기획서`: 9/1~9/2 현재 실행 항목과 활성 저장소·발표 범위를 상단에 추가했다.
- `05. DB 설계`, `04. 화면 설계`, `ASAK 처음 시작하기`: 코드·Figma·운영 DB를 추가로 확인해야 하는 범위라 수정하지 않았다.
