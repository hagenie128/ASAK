# Snapshot storage

Reserved for API and Notion snapshot outputs. Current `asak-data/scripts/notion_raw/` and root JSON snapshots remain in place because existing Python tools directly read and write them. Move only with approved script-path changes and a replay verification.
